// 스트림 반복자 사용하기
#include <numeric>;                                        // accumulate() 함수 - 범위에 속한 원소들의 합계
#include <iterator>;                                      // istream_iterator
#include <iostream>;                                      //

int main()
{
    std::cout << "값은 공백으로 구분해서 입력하세요. 종료하려면 Ctrl+Z를 입력하세요:" << std::endl;

    std::cout << "\n입력한 값의 합계는 "
        << std::accumulate.iterator<double>(), 0.0;)
        << std::endl;
}

